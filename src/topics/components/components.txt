function App(){
    return (
        <h1>Hello World</h1>
    )
}

export default App;

this is the first component

Breakdown :-
 1)
 function App()
 -- this creates component 
 2) 
 return ;
 -- this return UI

 -------------------------------

 For nested component 
 we will use <></> (fragment) or div
 because - we can only return ONE ROOT ELEMENT.